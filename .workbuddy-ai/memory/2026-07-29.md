# 2026-07-29

## Project: xian-BASIC-transaction_monitoring

### Task completed
Generated a pre-presentation PowerPoint (17 slides) at `Transaction_Monitoring_PrePresentation.pptx` using `python-pptx`. The script lives at `generate_pptx.py` so the user can regenerate after editing the content.

### Key facts captured
- Spring Boot 4.1 backend with JPA, MySQL persistence, four built-in rules (AMOUNT_THRESHOLD, VELOCITY_RULE, NEW_PAYEE_RULE, DAILY_LIMIT_RULE).
- Frontend is a single-file React (UMD) served as `static/index.html` — uses Recharts, Tailwind via CDN.
- API base paths: `/transactions`, `/api/alerts`, `/api/user/add/user`, `/api/monitoring-rules`.
- Database has 5 tables: transactions, alerts, monitoring_rules, alerts_status_history, User_table (known payees).
- Alert lifecycle: OPEN → ACKNOWLEDGED → INVESTIGATING → CLOSED/DISMISSED, with severity (HIGH/MEDIUM/LOW).
- Documentation: `transaction_monitoring.md` (full spec) + `Database_Design_Document/` + `docs/api/*.md`.
- Tests: JUnit 5 with H2 (test scope), `RulesTest`, `AlertServiceTest`, `TransactionRepositoryTest`.

### Slide deck outline (17)
1. Title  2. Agenda  3. Team  4. What we're building  5. Goals & scope  6. Alert lifecycle
7. Four rules  8. Architecture  9. Tech stack  10. Data model  11. REST API surface
12. Frontend mockup  13. Demo flow  14. Testing  15. Challenges  16. Next steps  17. Q&A

### Notes for next session
- The slide 03 "Team" slide is placeholder — user needs to swap in real team names and roles.
- The deck mirrors the `transaction_monitoring.md` presentation guidelines (story arc: who → what → how → demo → challenges → next).
- To re-run: `python generate_pptx.py` (depends on `python-pptx`).
